{
  "name": "eve-worker",
  "private": true,
  "scripts": {
    "start": "graphile-worker",
    "watch": "graphile-worker --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "graphile-worker": "0.12.2"
  }
}
